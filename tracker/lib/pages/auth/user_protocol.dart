import 'package:flutter/material.dart';

class UserProtocol extends StatelessWidget {
  const UserProtocol({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("用户协议页面..."),
    );
  }
}
