import 'package:flutter/material.dart';

class CarrierServiceProtocol extends StatelessWidget {
  const CarrierServiceProtocol({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("运营商服务协议页面..."),
    );
  }
}
